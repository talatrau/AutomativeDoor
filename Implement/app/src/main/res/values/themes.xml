<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AutomativeDoor" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="MyTimePicker" parent="android:Widget.Material.Light.TimePicker">
        <item name="android:textColorPrimary">@color/light_yellow</item>
        <item name="colorControlNormal">@color/cyan</item>
    </style>

    <style name="DialogActivity" parent="Theme.AppCompat.Dialog">
        <item name="windowMinWidthMajor">95%</item>
        <item name="windowMinWidthMinor">95%</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>